# Body Smith Admin

`universal-admin` 디자인 시스템과 구조를 유지하면서, 바디스미스 전용 블로그 관리자만 남긴 React 어드민입니다.

## Run

```bash
npm install
cp .env.example .env
npm run dev
```

## Environment

```bash
VITE_BASE_API_URL=http://localhost:8080/api/v1
```
